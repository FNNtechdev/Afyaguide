import {Link} from "react-router-dom";import {ArrowRight,Building2,MapPin,Navigation} from "lucide-react";import type {RankedFacility} from "../services/recommendation";import {useApp} from "../context/AppContext";import {t} from "../i18n/translations";export default function FacilityCard({facility:f}:{facility:RankedFacility}){const{language}=useApp(),tx=t[language];return <article className="card p-5 hover:shadow-md"><div className="flex items-start justify-between gap-4"><div className="flex min-w-0 gap-3"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-afya-50 text-afya-700"><Building2 size={22}/></div><div><h3 className="text-lg font-extrabold text-slate-900">{f.name}</h3><p className="text-sm text-slate-500">{f.facilityType} • {f.kephLevel}</p></div></div><div className="text-right"><div className="text-xl font-black text-afya-700">{f.score}%</div><div className="text-[11px] text-slate-400">Match</div></div></div><div className="mt-4 flex flex-wrap gap-2 text-xs font-semibold text-slate-600"><span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-3 py-1.5"><MapPin size={13}/>{f.county}</span><span className="rounded-full bg-slate-100 px-3 py-1.5">{f.subCounty}</span>{f.distanceKm!==undefined&&<span className="rounded-full bg-afya-50 px-3 py-1.5 text-afya-700">{f.distanceKm.toFixed(1)} km</span>}</div><div className="mt-4 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-afya-600" style={{width:`${f.score}%`}}/></div><p className="mt-3 text-sm text-slate-600"><strong>{tx.why}</strong> {f.reasons[0]}</p><div className="mt-5 flex gap-2"><Link to={`/facility/${f.id}`} state={{facility:f}} className="btn-primary flex-1 text-sm">{tx.details}<ArrowRight size={16}/></Link><a className="btn-secondary px-4" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(f.latitude!=null?`${f.latitude},${f.longitude}`:f.name)}`} target="_blank" rel="noreferrer"><Navigation size={18}/></a></div><p className="mt-3 text-[11px] text-slate-400">Demonstration data — verify information before visiting.</p></article>}